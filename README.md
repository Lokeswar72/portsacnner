# Network Port Scanner

A small Python multithreaded TCP port scanner. Use only on systems/networks you own or are authorized to test.

Run:
python -m venv .venv
.\.venv\Scripts\activate
python cli.py --host 127.0.0.1 --common